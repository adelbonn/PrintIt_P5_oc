//Sélection des flèches arrow_left et arrow_right
/*const arrowleft = document.querySelector('.arrow_left')
const arrowright = document.querySelector('.arrow_right')

//ajout d'un eventlistener sur la flèche gauche

arrowleft.addEventListener('click', () => {
    console.log('flèche gauche cliquée')
});

//ajout d'un eventlistener sur la flèche droite

arrowright.addEventListener('click', () => {
    console.log('flèche droite cliquée')
});

//Sélection du container des bullets points

const bullets = document.querySelector('.dots')

//importation du tableau slides
import { slides } from './script.js';

//ajout des bullets points
slides.forEach((slide, index) => {
    const bullet = document.createElement('div')
    bullet.classList.add('dot')
    if (index === 0) {
        bullet.classList.add('dot_selected') //ajoute la classe pour la photo en cours
    }
    bullets.appendChild(bullet) //ajoute le bullet point dans le container
    })*/
